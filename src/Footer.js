import React from "react";

function Footer() {
  return (
    <div
      className="footer"
      style={{
        display: "flex",
        justifyContent: "center",
        paddingBottom: "0.5rem",
        fontSize: "1rem",
        fontWeight: "bold",
      }}
    >
      <p>
        Created for Education/Practice purpose. Its not an Original NETFLIX
        Site.
      </p>
    </div>
  );
}

export default Footer;
